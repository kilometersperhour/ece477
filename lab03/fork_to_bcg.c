// Miles Martin
// March 7 2021
// Fork a process to the background, killing the parent process
// Restores terminal access with instructions to kill child process when finished

// Work In Progress!!

/*	int child = fork();
			int parent = getppid();

			printf("Child process: %d\n",child);
			printf("Parent process: %d\n",parent);

			if (getpid() == getppid()) {

				printf("Parent process ending!\n");
				return 0;

			} else {

				printf("Child process %d still alive.\n", child);
		*/